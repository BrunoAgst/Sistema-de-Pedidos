<%- include("./partials/header.ejs") %>
<body>
<%- include ('./partials/navbar.ejs') %>
<br>
<div class="container">
    <div class="card">
        <div class="card-header">
            <h3>Pedidos:</h3>
        </div>
        <% produtos.forEach(produto => { %>
        <div class="card-body">
            <% var data = produto.createdAt %>
            <h4>Data: <%= data.getDate() + "/" + data.getMonth() + "/" + data.getFullYear() %></h4>
            <h4>Nome: <%= produto.name %></h4>
            <h4>Quantidade: <%= produto.quantity %></h4>
            <h4>Kg: <%= produto.kg %></h4>
            <h4>Valor: R$<%= produto.amount %></h4>
        </div>
        <form action="/excluir-pedido" method="post">
            <input type="hidden" name="id" value="<%= produto.id %>">
            <button class="btn btn-primary">Excluir Pedido</button></a>
        </form>
        <hr>
        <% }) %> 
    </div>
</div>  
</body>
<%- include("./partials/footer.ejs") %>